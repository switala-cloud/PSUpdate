# PSUpdate
PowerShell-based Windows Update automation for image pipelines. Replaces legacy WUA COM usage with PSWindowsUpdate, supports regex exclusions, and provides clean reboot signaling (exit 101) for Packer compatibility.
